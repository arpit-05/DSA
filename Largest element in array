import java.util.* ;
import java.io.*; 

public class Solution {

    static int largestElement(int[] arr, int n) {
    
    int max=arr[0];
       for(int i=0;i<n-1;i++){
           
           if(arr[i+1]>max)
            max=arr[i+1];
       }
       return max;

    }
}
